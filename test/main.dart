import 'cloud.dart';
import 'package:http/browser_client.dart';
import 'dart:typed_data';

main() async {
//  var client = new BrowserClient();
//  var api = new CloudApi(client);
//
//  var request = new MediaMessage();
//  request.bytes = new Uint8List(1);
//  request.updated = new DateTime.now();
//  api.method(request);
}